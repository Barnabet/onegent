name: router
version: 0.1.0
owner: team-platform-ai

description: >
  Meta-pack that fronts the user. Its only job is to decide which specialist
  pack should handle a request and delegate via orchestrator.delegate. It
  has no domain tools of its own — see skills/router/SKILL.md for the
  routing rules.

skills:
  - router

classification: public
allowed_data_sources: []

model: claude-sonnet-4-6

limits:
  max_tool_calls_per_run: 20
  max_tokens_per_run: 50000
  timeout_seconds: 300
